spring.application.name=limits-service
# also can add a specific port
#limits-service.minimum=1
#limits-service.maximum=300
# renamed this file from application.properties -> bootstrap.properties.
#Spring cloud config server uri to pick the properties from.
spring.cloud.config.uri=http://localhost:8888
#configure profile, note that this profile is generally passed in via the commandline as a java argument.
spring.profiles.active=dev
